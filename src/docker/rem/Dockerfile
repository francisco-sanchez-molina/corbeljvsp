FROM java:8u45

MAINTAINER Corbel Team @ bq <corbel-dev@bq.com>

ADD ${project.name}-${project.parent.version}.jar /resources/plugins/${project.name}/${project.name}.jar

VOLUME /resources/plugins/${project.name}